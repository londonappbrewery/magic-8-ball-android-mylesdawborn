<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/smokeBlue</item>
        <item name="colorPrimaryDark">@color/darkSmokeBlue</item>
        <item name="colorAccent">@color/lightSmokeBlue</item>
        <item name="android:textColorPrimary">@color/lightGrey</item>
        <item name="android:colorBackground">@color/semiBlack</item>
        <item name="android:colorForeground">@color/foreground_material_light</item>
    </style>

</resources>
